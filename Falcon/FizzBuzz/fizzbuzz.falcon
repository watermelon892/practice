function fizzbuzz(i)
  switch i % 15
  case 0           : > "FizzBuzz"
  case 3, 6, 9, 12 : > "Fizz"
  case 5, 10       : > "Buzz"
  default          : > i
  end
end

function run(n)
  for i in [1:n+1]
    fizzbuzz(i)
  end
end

run(15)
